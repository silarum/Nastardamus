-- The corrected weighted selector is part of the preceding wheel migration.
-- This marker preserves the production migration sequence after the live
-- function was replaced before the application release.
select 1;
